# Server

Install dependencies:
```
cd server
npm install
```

Set environment variables (copy .env.example to .env and fill values). Start:
```
npm start
```

The server exposes:
- GET /api/services
- POST /api/bookings
- GET /api/health
